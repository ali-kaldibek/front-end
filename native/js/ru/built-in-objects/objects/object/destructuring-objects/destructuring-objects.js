const userProfile = {
    name: 'Bogdan',
    commentsQty: 23,
    hasSighedAgreements: false,
};

const { name, commentsQty, hasSighedAgreements } = userProfile;

console.log(name);

console.log(userProfile.name);